postsubmits:
- name: publish-binaries-to-gh-release
  decorate: true
  branches:
    - cargo-fslabscli-.*
  spec:
    containers:
    - image: ghcr.io/foresightminingsoftwarecorporation/prow-tests:1d889a5fa6d02b9a86c031acb6a316532f08d5f0@sha256:afec0c298bc9f0ef6046aebcf0c7023d9ab5f0155668163265029c9bdef3fa30
      command:
        # - bash
        # - -c
        # - "grep 'nixbld' /etc/group && for i in $(seq 1 10); do usermod -a -G nixbld -u $((30000 + $i)) nixbld$i; done && grep 'nixbld' /etc/group && nix build .#release"
        # # - id
        # # - nixuser
        - nix
        - build
        - .#release
      # securityContext:
        # fsGroup: 30000
